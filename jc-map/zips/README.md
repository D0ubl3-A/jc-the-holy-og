# JC Map ZIP uploads

Upload JC Map ZIP packages here. GitHub's browser uploader accepts ZIP files, but each file must stay within GitHub's web-upload size limits. For larger source archives, split them into smaller ZIP parts before uploading.
